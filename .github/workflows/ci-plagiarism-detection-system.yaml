name: continuous integration - plagiarism-detection-system

concurrency:
  group: plagiarism-detection-system
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: ["*"]
    paths: ["apps/plagiarism-detection-system-server"]

jobs:
  continuous-integration:
    name: continuous integration
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: setup node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - name: install dependencies
        run: npm install
        working-directory: apps/plagiarism-detection-system-server

      - name: build
        run: npm run build
        working-directory: apps/plagiarism-detection-system-server

      - name: test
        run: npm test
        working-directory: apps/plagiarism-detection-system-server
